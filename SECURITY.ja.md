# セキュリティポリシー

English version: [SECURITY.md](SECURITY.md)

## 脆弱性の報告

**セキュリティ上の問題を public issue に書かないでください。**

GitHub の非公開報告機能（private vulnerability reporting）を使ってください。

1. このリポジトリの [Security タブ](https://github.com/siosig/genai-rs/security) を開く
2. **Report a vulnerability** を選ぶ
3. 下の「報告に含めてほしいこと」に沿って記述する

この経路なら修正が用意できるまで報告が非公開のまま保たれる。本ポリシーに
メールアドレスを書いていないのは意図的で、公開アーカイブに載ったり迷惑メール
フォルダに落ちたりする経路を作らないため。

GitHub が使えない場合は、技術的詳細を一切書かずに「セキュリティの件を非公開で
報告したい」とだけ書いた issue を立ててもらえれば、メンテナが非公開の経路を
用意する。

## 報告に含めてほしいこと

揃っているほど修正が早くなる。

- クレートのバージョン（またはコミット）と、有効にしていた Cargo feature
- 影響する箇所: HTTP トランスポート / Live API の WebSocket / ファイルアップロード /
  自動関数呼び出し / MCP ブリッジ / 生成された型 のどれか
- 攻撃者が何を得るのか、そのために何が必要か（悪意あるAPIレスポンス？ 細工した
  ファイル？ ベース URL の掌握？）
- 再現手順。失敗するテストの形だと一番早い
- 上流の [Google Gen AI Python SDK](https://github.com/googleapis/python-genai)
  にも同じ問題があるか。本クレートは同 SDK の移植なので、設計を共有している
  部分のバグは上流にも報告が要る。その旨を書いてもらえれば、上流より先に公開
  しないよう調整する

## 応答の目安

| 段階 | 目安 |
| --- | --- |
| 受領の一次応答 | 72 時間以内 |
| 深刻度の判定と対応方針の提示 | 7 日以内 |
| 修正のリリース、または修正不能な場合の公開アドバイザリ | 90 日以内 |

個人が余暇で維持しているプロジェクトなので、保証ではなく目安。72 時間たっても
反応がなければ、非公開報告のスレッドに催促のコメントをもらえると助かる。

## 対象範囲

本クレートは Gemini Developer API と通信するクライアントライブラリ。対象に含まれるもの:

- API キーの漏洩。ログ・エラーメッセージ・パニックのペイロード・`Debug` 出力、
  その他呼び出し側が転送しうる場所への流出
- 意図しないホストへの認証情報やユーザーコンテンツの送信。呼び出し側が指定した
  ベース URL やリダイレクトの検証不足など
- 悪意ある、または不正な形式の API レスポンスから到達できるメモリ安全性・
  リソース枯渇・パニック。`unsafe_code` はクレート全体で deny しているので、
  健全性のバグが出るなら想定外であり、報告する価値が高い
- 自動関数呼び出しや MCP の経路で、モデルの応答が意図しない呼び出しを引き起こすもの
- 本リポジトリ固有の依存・ビルド時の供給網の問題。ピンされていない Action、
  検証なしのダウンロード、ピン済みアーティファクトの改竄など

対象外:

- Gemini API 自体の脆弱性。Google に報告してほしい
- サードパーティ依存の既知の脆弱性で、上流に修正がすでにあるもの。通常の issue
  （または Dependabot の PR）で扱う
- モデルの出力そのもの。有害な出力はモデルの問題であり、トランスポート層の
  ライブラリの問題ではない
- ライブラリを動かしているマシン、または API キーを読む環境をすでに掌握している
  攻撃者を前提とするもの

## サポート対象バージョン

1.0 未満。修正は最新リリースにのみ入る。過去バージョンへのバックポートはしない。

## 本クレートにおける API キーの扱い

報告の前に知っておくと役立つ、意図的な設計。

- API キーは `secrecy::SecretString` で保持しており、`Debug` 実装は値を伏せる
- HTTP リクエストはキーを `x-goog-api-key` ヘッダで送るので、URL には現れない
- Live API だけは例外で、WebSocket のプロトコル上キーが URL のクエリ文字列に
  載る。これは上流 SDK と同じで、本クレート側では変えられない。その URL は
  ログにもエラーメッセージにも出しておらず、コード上にもその旨を明記している。
  もし漏れる経路を見つけたなら、それこそが本ポリシーの想定する報告
