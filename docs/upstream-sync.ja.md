# 上流追従状況

手書きで管理する（`parity.md` と違い生成物ではない。上流ピンを変更するたびに手で更新する）。将来の担当者が知りたくなる2つの問いに答える: 「いま実際にどの上流状態にピンしているか」と「上流にあると分かっていて意図的に取り込んでいないものがあるか、あるならなぜか」。

## 目次

- [現在のピン](#現在のピン)
- [意図的な持ち越し](#意図的な持ち越し)
- [恒久的な対象外](#恒久的な対象外)

## 現在のピン

| 上流バージョン | 上流参照 | 追従した日 | feature |
|---|---|---|---|
| 2.19.0 | `66807187` | 2026-08-22 | 001-port-genai-rust |
| 2.23.0 | `e384b55`（タグ `v2.23.0`） | 2026-09-12 | 003-upstream-2-23-sync |

`tools/codegen/upstream.py` の `PINNED_VERSION` がこの表の唯一の情報源。バージョンを上げる具体的な手順はそのファイル自身のヘッダコメントを参照。

## 意図的な持ち越し

上流にあると分かっていて取り込んでいない変更。「いずれ」ではなく、実際に観測できる再開条件を添える。

| 上流コミット | 変更内容 | 見送った理由 | 再開条件 | 利用者への影響 |
|---|---|---|---|---|
| `2580638` | 自動関数呼び出し: リモート呼び出しの予算が尽きたら、呼び出し元の関数を実行しない（従来は結果を送る先のリクエストが残っていないのに実行していた） | v2.23.0 タグの後のコミットで、この追従時点で PyPI に公開されていない（公開済み最新は 2.23.0、2.24.0 は未公開）。PyPI 公開を超えてピンするにはバージョンではなくコミット SHA でピンする必要があり、codegen パイプラインのハッシュ検証付き依存ロック（`tools/codegen/requirements.txt`、`--require-hashes`）がその形に対応していない | 上流が 2.24.0（またはそれ以降）を PyPI に公開したら | **あり。** リモート呼び出しの予算が尽きたとき、本クレートは登録された関数を1回多く実行してしまい、その結果を送る先のリクエストは残っていない。その関数に副作用（課金・メッセージ送信・記録の書き込み）があれば、その副作用は実際に起きる。`src/afc.rs` を参照 |

この表が空であることは正常な状態であり、「表を書き忘れた」ことの証拠ではない。現時点で持ち越しているものが無いことを意味する。

## 恒久的な対象外

「まだ」ではなく、意図的に移植しないと決めたもの。決定した理由とともに記す。

| 領域 | 理由 | 決定した feature |
|---|---|---|
| Vertex AI バックエンド | 本クレートは Gemini Developer API のみを対象とする。Vertex AI を要求（`vertexai(true)`、`project`/`location`、`GOOGLE_GENAI_USE_VERTEXAI`）すると即座に `Error::UnsupportedBackend` で失敗する | 001 |
| Interactions API / `_gaos`（environments、agents、webhooks、triggers） | 上流が活発に拡張している別の API 面（2.19.0→2.23.0 の差分の大半はここにある）。将来移植する場合は別 feature に委ねる | 001 |
| `local_tokenizer` | 未移植 | 001 |
| `_replay_api_client` | 上流自身のテスト再生基盤。本クレートは独自のゴールデンフィクスチャ機構（`tools/codegen/gen_fixtures.py`）でパリティを証明する | 003 |

「恒久的」は不変を意味しない。とくに Interactions は将来の feature で対象になりうる。そうなった場合はこの行を削除するのではなく、「現在のピン」に変更を説明する新しい注記を添えて移動すること。
